# Quickstart — Examples

## User prompt

```
I'm new to agent-loom. Run the quickstart demo.
```

## Agent response (abbreviated)

```markdown
## Quickstart complete

Result: KEPT
Verify: pytest → pass (2 tests)
What happened: safe-change added a zero guard to divide() after mapping impact on the seed calc fixture.
Next: browse examples/safe-change-demo/, examples/structured-planning-demo/, examples/deploy-anywhere-demo/
```
