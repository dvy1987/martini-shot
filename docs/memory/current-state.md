# Current State

**Where:** 2026-09-02 — Stage 1 spine through **Gate G2 GREEN**. Track A, B-1, B-2, B-3, C-1 contract remainder, D-1..D-7 (pickups identity/EDD, no Veo) are in the working tree pending logical commits + push (`HEAD` was `c2ecb36`, `origin/main` `4aaa6d5` before this push).
**Blocking:** none for Stage 1 stations. Owner should rotate API keys (G1 SSE query-string leak) and restart local uvicorn after pull so spine/present match git.
**Next queue:** Phase 3 hero/Dub QC per tasks file; hosted MCP OAuth (F-4) deferred; Stage 1a parked behind G3.
**Worker lane (A6):** read-only research only.
**Open:** F-4 OAuth persistence; Grafana Cloud `create_incident` FK on this org; spec §7.2 Firebase sign-in at H-3; Replit static deploy until owner approves the runbook.
