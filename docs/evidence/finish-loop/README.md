# Finish-loop evidence

Walk-away finishing: billed inspect, billed orchestrator rank (tradeoffs +
dependencies), ADK team, pause-assemble.

- `inspect_eval.jsonl` — live inspect: Gemini watches real clips (3 runs)
- `rank_eval.jsonl` — live orchestrator rank (3 runs)

Do not count Veo-finished rows as an Omni pass.
