# D-5 Pickups eval (INPUT flicker)

Identity-path flicker on 10 labeled INPUT clips. No Veo/Omni calls. Cost printed `$0.00`.

Threshold `mean_flicker_score < 0.18` (`backend/evals/thresholds.yaml`). This run: **PASS** (see `summary.json`).
