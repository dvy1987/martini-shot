# fixtures/finish_inspect — labeled INPUT for finishing inspect EDD (C-1.3)

Clips are **built at eval time** in `docs/evidence/finish-loop/clips/` (gitignored):

- Picture: original Omni tapes already on GCS (kitchen cooks, table two-shot,
  cafe sign) — not Big Buck Bunny, not gradients.
- Hearability rows: real Chirp 3 HD speaks a known line, muxed quiet vs full.
- Dark-faces / mid-cut / CLOSED sign: ffmpeg carries the actual deficit in
  pixels.

The inspect agent is scored on a **watch** (frames + wav), same as production.
Do not treat these muxes as locked cuts.
