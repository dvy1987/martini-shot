# Pickups eval INPUT clips

Labeled synthetic INPUT only (C-1.3). `derived/` holds ffmpeg trims produced by
`scripts/pickups_eval.py` from Big Buck Bunny / spike shots. Do not treat those
files as generated QC output.
