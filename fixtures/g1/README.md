# G1 ingest fixture

Labeled synthetic INPUT only (C-1.3). Not a generated QC result.

| File | What it is |
|---|---|
| `slate.mp4` | 1-second 320×240 color slate (`testsrc`), H.264 + AAC, produced with the project's ffmpeg binary. Used as the Gate G1 real-MP4 upload. |

Do not treat this file as a locked cut or as station output.
