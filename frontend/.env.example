# Vite env vars are VITE_-prefixed and baked in at BUILD time from
# frontend/.env.local (gitignored). Never commit real values.
# Two hosts (Firebase interim / Replit final) = two build-time base URLs.
VITE_API_BASE_URL=http://localhost:8000
VITE_API_KEY=
