"""Specialist agent personas (Amendment A9, H-1b..H-1e).

Each module here implements ONE specialist: a real Gemini call through the
single instrumented site (`otel_ai.run_agent_call`), a typed finding schema
validated deterministically, and read-only Grafana MCP tools (C-2.1 —
specialists hold zero act-class tools; only the H-0 executor acts).
"""
